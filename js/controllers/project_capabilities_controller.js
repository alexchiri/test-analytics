App.ProjectCapabilitiesController = Ember.ArrayController.extend({
	actions: {
		// addCapability: function() {
		// 	var capabilityName = this.get('newcapabilityName');
		// 	if(!capabilityName.trim()) { return; }
	
		// 	var model = this.get('model');
		// 	var project = this.get('project');
	
		// 	var component = this.store.createRecord('component', {
		// 		name: capabilityName,
		// 		description: '',
		// 		project: project,
		// 		capabilities: []
		// 	});
	
		// 	this.set('newcapabilityName', '');
	
		// 	component.save();

		// 	// push component to the project array of component
		// 	model.pushObject(component);
		// }
	}	
});